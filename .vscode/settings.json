// ARQUIVO DE CONFIGURAÇÃO DO VSCODE NESSE PROJETO
{
    // Aumenta o zoom do VSCODE
    // "window.zoomLevel":0

    // Configuração do Code Runner e ambiente pronto para rodar scripts Node
    "code-runner.executorMap": {
        // Include e Exlude no tsconfig.json
        "typescript": " clear && npx ts-node --files --transpile-only",
    }
}